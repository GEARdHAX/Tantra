name: agroproject
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:  # ✅ Add this
    sdk: flutter
  intl: ^0.20.2           # ✅ Keep intl for localization

  geolocator: ^14.0.2
  http: ^1.1.0
  wave: ^0.2.0
  fl_chart: ^0.68.0
  flutter_tts: ^4.0.2
  url_launcher: ^6.3.0
  camera: ^0.11.0+2
  image_picker: ^1.1.1
  universal_html: ^2.2.4
  cupertino_icons: ^1.0.8
  web_socket_channel: ^2.4.0
  flutter_dotenv: ^5.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true   # ✅ This enables `flutter gen-l10n`

  assets:
    - assets/
    - .env
